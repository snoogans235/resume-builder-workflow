# Local career evidence

The personal career evidence library is intentionally excluded from this
repository. Keep it locally at `references/evidence/` in the installed
`job-application` skill.

The installer preserves an existing local evidence directory, and the sync
script never copies that directory into Git.
